<!doctype html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>UAS VIRTUAL</title>
    <link rel="icon" type="image/svg+xml" href="" />
    <meta name="generator" content={Astro.generator} />
</head>
<body>
    <nav id="navbar" aria-label="Main navigation">
        <div class="nav-left">
            <img src="/UAS_logo.png" id="Logo_UAS" alt="UAS logo" />
            <img src="/logouas2.png" id="Logo_UAS_Virtual" alt="UAS virtual" />
            <h3>Universidad Autónoma de Sinaloa</h3>
        </div>

        <div class="nav-right">
            <ul>
                <li><a href="/">Inicio</a></li>
                <li><a href="/facultad">Facultad</a></li>
                <li><a href="/oferta_educativa">Oferta Educativa</a></li>
                <li><a href="/departamentos">Departamentos</a></li>
                <li><a href="/comunidad">Comunidad</a></li>
            </ul>
        </div>
    </nav>

    <main id="container">
        <slot />
    </main>

    <footer id="footer">
        <div class="footer-section">
            <h4>Universidad Autónoma de Sinaloa</h4>
            <p>Construyendo el futuro a través de la educación y la tecnología.</p>
        </div>

        <div class="footer-section">
            <h4>Enlaces</h4>
            <ul>
                <li><a href="/">Inicio</a></li>
                <li><a href="/facultad">Facultad</a></li>
                <li><a href="/oferta_educativa">Oferta Educativa</a></li>
                <li><a href="/departamentos">Departamentos</a></li>
            </ul>
        </div>

        <div class="footer-section">
            <h4>Redes Sociales</h4>
            <div class="social-icons">
                <a href="#" aria-label="Facebook"><img src="../../public/Redes_sociales/facebook.png" alt="Facebook"></a>
                <a href="#" aria-label="X"><img src="../../public/Redes_sociales/x.png" alt="X"></a>
                <a href="#" aria-label="Instagram"><img src="../../public/Redes_sociales/instagram.png" alt="Instagram"></a>
                <a href="#" aria-label="YouTube"><img src="../../public/Redes_sociales/youtube.png" alt="YouTube"></a>
            </div>
        </div>
    </footer>

</body>
</html>

<style>
    /* LAYOUT PRINCIPAL */
    :root{
        --nav-bg: #021846;
        --accent: #ffd700;
        --text: #e5e7eb;
        --page-bg: #f8fafc;
    }

    html, body {
        margin: 0;
        padding: 0;
        font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
        background: var(--page-bg);
        box-sizing: border-box;
    }

    /* Hacer que footer quede abajo */
    body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
    }

    main#container {
        flex: 1; /* ocupa el espacio disponible */
        padding: 1.25rem;
        min-height: 0; /* evitar overflow en flex */
    }

    /* NAV */
    #navbar{
        background: var(--nav-bg);
        color: var(--text);
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        padding: 10px 20px;
    }

    .nav-left, .nav-right {
        display: flex;
        align-items: center;
        gap: 12px;
    }

    /* logo y tamaños — usa max-width para que sean responsivos */
    #Logo_UAS, #Logo_UAS_Virtual {
        max-width: 90px;
        height: auto;
        display: block;
    }

    /* Título dentro del nav */
    #navbar h3 {
        margin: 0;
        font-size: 1.05rem;
        color:#ffd700;
        font-weight: 600;
    }

    /* Menú (derecha) */
    .nav-right ul {
        margin: 0;
        padding: 0;
        list-style: none;
        display: flex;
        gap: 1rem;
        align-items: center;
    }

    .nav-right a {
        color: var(--text);
        text-decoration: none;
        padding: 0.35rem 0.6rem;
        border-radius: 6px;
        transition: background-color 0.15s ease, color 0.15s ease;
        display: block;
        white-space: nowrap;
    }

    .nav-right a:hover {
        background: rgba(255,255,255,0.06);
        color: var(--text);
    }

    /* FOOTER */
    #footer {
        background: var(--nav-bg);
        color: var(--text);
        padding: 30px 40px;
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 30px;
    }

    #footer .footer-section {
        flex: 1;
        min-width: 220px;
    }

    #footer h4 {
        margin-bottom: 10px;
        color: var(--accent);
    }

    #footer ul {
        list-style: none;
        margin: 0;
        padding: 0;
    }

    #footer ul li { margin-bottom: 6px; }

    #footer a { color: var(--text); text-decoration: none; transition: 0.2s; }
    #footer a:hover { color: var(--accent); }

    .social-icons { display: flex; gap: 15px; align-items:center; }

    .social-icons img {
        width: 28px;
        height: 28px;
        transition: filter 0.2s, transform 0.15s;
    }

    .social-icons img:hover { transform: translateY(-2px); filter: none; }

    /* RESPONSIVE: colapsa el nav en pantallas pequeñas */
    @media (max-width: 720px) {
        .nav-right ul { gap: 0.6rem; font-size: 0.95rem; }
        #Logo_UAS, #Logo_UAS_Virtual { max-width: 64px; }
        #footer { padding: 20px; }
    }
</style>
